println()color(0x00DDFF)println()println("                   ***** emuMMC joy-con Pairing Sync *****")println()println()color(0x40FF00)println("Dieses Script synchronisiert den emuMMC mit dem sysMMC, damit die Nutzung des")println("Controllers unter beiden Nands ermoeglicht werden kann, ohne diesen")println("erneut synchronisieren zu muessen. Die Anwendung ist nur erforderlich,")println("wenn einer der beiden Nands einmal auf die Werkseinstellungen zurueckgesetzt wurde.")println()println()color(0xFFFFFF)println("Switch Lite und Tablet Nutzer:")println()println("Vol + / -    : navigieren / abbrechen / beenden")println("Power Button : bestaetigen")println()println()color(0xFFFF40)println("Druecke die A Taste, um den emuMMC mit dem sysMMC zu synchronisieren")println()color(0xE70000)println("Druecke (B) zum Beenden.")color(0xFFFFFF)buttons=pause().raw if(buttons&0x4){exit()}JoyX=0x2;if(pause().raw&JoyX){noemu=(emu()==0);if(noemu){color(0xE70000);println("EMUMMC nicht gefunden!");println("");color(0xFF8C00);println("Druecke (B) zum Beenden.");pause().raw;exit();}}println("");println("Mounte sysMMC...");if(mountsys("SYSTEM")){color(0xE70000);println("Das hat leider nicht funktioniert!");println("");color(0xFF8C00);println("Druecke (B) zum Beenden.");pause().raw;exit();}color(0xFFFFFF);println("Mounte SYSTEM...");mountsys("SYSTEM");mkdir("sd:/tegraexplorer");mkdir("sd:/tegraexplorer/syncfix");mkdir("sd:/tegraexplorer/syncfix/backup");println("Sichere SYSMMC 8000000000000050 Datei nach sd:/tegraexplorer/syncfix/backup");copyfile("bis:/save/8000000000000050","sd:/tegraexplorer/syncfix/backup/8000000000000050_sysmmc_backup");color(0x8040FF);println("Mounte emuMMC");if(mountemu("SYSTEM")){color(0xE70000);println("Das hat leider nicht funktioniert!");println("");color(0xFF8C00);println("Druecke (B) zum Beenden.");pause().raw;exit();}println("Mounte SYSTEM...");mountemu("SYSTEM");println("Sichere EMUMMC 8000000000000050 Datei nach sd:/tegraexplorer/syncfix/backup");println("");copyfile("bis:/save/8000000000000050","sd:/tegraexplorer/syncfix/backup/8000000000000050_emummc_backup");println("Kopiere 8000000000000050 Datei vom SYSMMC nach EMUMMC");println("");copyfile("sd:/tegraexplorer/syncfix/backup/8000000000000050_sysmmc_backup","bis:/save/8000000000000050");color(0x40FF00);println("FERTIG! ");println("");println("Jetzt sollten Deine Joy-Cons zwischen SYSMMC und EMUMMC synchronisiert sein");println("");color(0xFF8C00);println("Druecke (B) zum Beenden.");pause().raw;exit();